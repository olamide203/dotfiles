---
session_name: proc360-admin
start_directory: ~/workspace/checkit/proc360-admin/
windows:
  - window_name: frontend
    layout: main-vertical
    options:
      main-pane-width: 50%
    panes:
      - nvim .
      - shell_command:
          - cmd: npm install
          - cmd: npm run dev
      - pane
